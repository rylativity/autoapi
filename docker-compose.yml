version: '3.9'

services:
  trinoautoapi:
    build: .
    image: rms113/trinoautoapi:0.0.1
    ports:
      - 8000:8000
    volumes:
      - ./:/app
    environment:
      - TRINO_HOST=trino
      - TRINO_PORT=8080
      - TRINO_USER=trino
      - DEBUG=true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    # depends_on:
    #   - postgres
  # postgres:
  #   image: postgres:latest
  #   ports:
  #     - 5432:5432
  #   environment:
  #     - POSTGRES_USER=autoapi
  #     - POSTGRES_PASSWORD=autoapi
  #     - POSTGRES_DB=autoapi
  #   # volumes:
  #   #   - postgres-db:/var/lib/postgresql/data
  #   #   - ./postgres_init_scripts/:/docker-entrypoint-initdb.d/
  #   healthcheck:
  #     test: ["CMD", "pg_isready","-U", "hive", "metadata"]
  #   restart: always

# volumes:
#   postgres-db:
# networks:
#   default:
#     name: container-analytics-platform
#     external: true